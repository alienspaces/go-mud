apiVersion: v1
kind: Service
metadata:
  name: {APP_PROJECT_NAMESPACE}-template-service
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  ports:
  - port: 443
    protocol: TCP
    name: https
    targetPort: 8080
  selector:
    app: {APP_PROJECT_NAMESPACE}-template
